import SquareImageTool from "./SquareImageTool";

export const metadata = {
  title: "Square Image - SwiftPic",
  description: "Convert Image to Square with Custom Background.",
};

const page = () => {
  return (
    <div className="">
      <SquareImageTool />
    </div>
  );
};

export default page;
